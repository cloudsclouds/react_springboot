<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.server_springboot.collabdocument.mapper.DocumentMapper">

  <insert id="insertDocument" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO documents (title, owner_id, owner_name, latest_snapshot, version, status, created_at, updated_at)
    VALUES (#{title}, #{ownerId}, #{ownerName}, #{latestSnapshot}, 1, 0, NOW(), NOW())
  </insert>

  <select id="findById" resultType="com.example.server_springboot.collabdocument.entity.Document">
    SELECT * FROM documents WHERE id = #{id}
  </select>

  <select id="findUserDocuments" resultType="com.example.server_springboot.collabdocument.dto.DocumentResponse">
    SELECT 
      d.id, 
      d.title, 
      d.owner_id as ownerId, 
      d.owner_name as ownerName,
      dm.role as myRole, 
      d.updated_at as updatedAt
    FROM documents d
    INNER JOIN document_members dm ON d.id = dm.document_id
    WHERE dm.user_id = #{userId}
      AND dm.role IN ('owner', 'editor', 'viewer')
      AND d.status = 0
    ORDER BY d.updated_at DESC
  </select>

  <update id="updateTitle">
    UPDATE documents SET title = #{title}, updated_at = NOW() WHERE id = #{id}
  </update>

  <update id="updateSnapshot">
    UPDATE documents SET latest_snapshot = #{latestSnapshot}, updated_at = NOW() WHERE id = #{id}
  </update>

  <update id="updateStatus">
    UPDATE documents SET status = #{status}, updated_at = NOW() WHERE id = #{id}
  </update>

</mapper>
